import React from "react";
import styled from "styled-components";

const StyledTitle = styled.h1`
  font-size: 40px;
  line-height: 52px;
`;

function Title() {
  return <StyledTitle>Reach out to us!</StyledTitle>;
}

export default Title;
